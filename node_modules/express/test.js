
var express = require('./')
  , app = express();

app.set('json replacer', function(key, value){
  if ('_' == key[0]) return;
  return value;
});

var user = { name: 'Tobi' };

app.get('/user', function(req, res){
  res.send(user);
});

app.listen(3000);
